Repository placeholders for offline-first implementation.

Files to add here:
- `product_repository.dart` (done)
- `transaction_repository.dart` (create next)

These repositories should accept `DatabaseHelper` and `PostgresApiService` as dependencies.
