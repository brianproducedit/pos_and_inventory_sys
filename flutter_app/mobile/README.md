# POS & Inventory — Mobile App

Flutter mobile application (Android-first).

See the [root README](../../README.md) for full setup instructions and architecture.

## Quick dev commands

```bash
# Get dependencies
flutter pub get

# Generate code (Drift tables, envied config)
dart run build_runner build --delete-conflicting-outputs

# Run on connected device
flutter run
```